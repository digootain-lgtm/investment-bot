services:
  - type: web
    name: investment-bot
    env: python
    buildCommand: bash build.sh
    startCommand: python investment_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
